spring:
  r2dbc:
    url: r2dbc:mysql://localhost:3306/db?useSSL=false
    username: user
    password: user
  kafka:
    consumer:
      auto-offset-reset: earliest
kafka:
  bootstrap: localhost:9092
server:
  port: 7000
logging:
  level:
    org:
      springframework:
        transaction:
          interceptor: TRACE
