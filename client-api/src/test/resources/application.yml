spring:
  r2dbc:
   url: r2dbc:tc:mysql:///db?TC_IMAGE_TAG=5.7
  sql:
    init:
      mode: always
  cloud:
    consul:
      enabled: false
    config:
      enabled: false
logger:
  default: com.frizzer.logger
angular:
  origins: http://localhost:4200